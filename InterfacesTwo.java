package chaptersix;
// An interface extending another interface for a class to override
interface InterfacesTwo extends Interfaces {
  // interfaces do not have any constructors. All vairables within an interface are public, static, and final.
  int noSpeed = 0;
  int maxSpeed = 150;
}
